#if 'jack_capture' in $data['wizard.sectionnamelist']
#for $username in $data['jack_capture.userslist']
 <connection to="jack_capture-$username:input1" from="Jamulus $username:output left"/>
 <connection to="jack_capture-$username:input2" from="Jamulus $username:output right"/>
#end for
#end if
